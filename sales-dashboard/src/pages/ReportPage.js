import React from 'react';
import { Layout } from 'antd';
const { Content } = Layout;

const ReportPage = () => (
  <Content style={{ padding: '20px' }}>
    {/* Add components relevant to the Report page here */}
    <div>Report Page Content</div>
  </Content>
);

export default ReportPage;
